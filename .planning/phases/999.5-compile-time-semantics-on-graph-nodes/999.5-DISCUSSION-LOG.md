# Phase 999.5: Compile-Time Semantics on Graph Nodes - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-19
**Phase:** 999.5-compile-time-semantics-on-graph-nodes
**Areas discussed:** Summary Granularity, Context Assembly Strategy, Relationship Schema in LanceDB

---

## Summary Granularity

| Option | Description | Selected |
|--------|-------------|----------|
| Option A | Node & Edge Level — Summarize both entity nodes and relationship edges. | ✓ |
| Option B | Node-Level Only — Summarize entities only, keep edges simple. | |
| Option C | Community-Level Only — Rely entirely on Phase 999.1 community summaries. | |

**User's choice:** Option A (Node & Edge Level)
**Notes:** We will generate pre-computed summaries for both entities (nodes) and relationships (edges) to preserve topological details when traversing.

---

## Context Assembly Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Option A | Summary-Only — Inject only pre-computed summaries, ignore raw chunks. | |
| Option B | Hybrid — Retrieve both summaries and raw chunks. | |
| Option C | Fallback-Enabled — Use summaries, fall back to raw chunks if missing. | ✓ |

**User's choice:** Option C (Fallback-Enabled)
**Notes:** The traverser prioritizes pre-computed summaries. If they are missing (e.g. background job is still processing), it gracefully falls back to raw chunks. This ensures query robustness.

---

## Relationship Schema in LanceDB

| Option | Description | Selected |
|--------|-------------|----------|
| Option A | Separate Edges Table — Nodes and edges tables, both with vector columns. | ✓ |
| Option B | Node Adjacency List — Store relationships inside node rows. | |

**User's choice:** Option A (Separate Edges Table)
**Notes:** Treating relationships as first-class database tables (`edges` table in LanceDB) allows us to store summaries and vectors on edges, enabling semantic vector searches over relationships.

---

## the agent's Discretion

- Edge summarization prompts, fallback threshold settings, and cache management for traversers.

## Deferred Ideas

- None — discussion stayed within the backlog phase scope.
